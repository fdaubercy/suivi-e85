# Lessons Learned

| Date | Problème | Règle
|---|---|---|
| 2026-06-08 | Modules dupliqués après import .bas | Toujours vérifier VB_Name dans .bas avant import, utiliser set-module plutôt qu'import |
| 2026-06-14 | Erreur compilation VBAProject sur ligne Attribute VB_Name | Avant set-module, filtrer les lignes `^Attribute VB_Name` du .bas (métadata export uniquement, invalide comme code VBE) |
| 2026-06-08 | Ghost references dans le binaire VBA | Après suppression de module, sauvegarder et rouvrir le classeur avant toute exécution |
| 2026-06-08 | UTF-8 corruption dans commentaires VBA | Les fichiers .bas doivent être encodés en CP1252 (Windows-1252) pour Excel VBA |
| 2026-06-08 | 0x800ADF09 après modifications COM | Fermer/rouvrir Excel pour forcer la recompilation du p-code VBA |
| 2026-06-08 | ChrW(&H1F50D&) cause "Invalid procedure call" (emoji hors BMP, valeur > 65535) | ChrW() n'accepte que 0–65535 (BMP). Pour les emoji 4-octets (U+1F...), utiliser une alternative BMP ou supprimer le caractère |
| 2026-06-08 | Lignes `Attribute VB_Name = "..."` dans un .bas provoquent "Impossible d'exécuter la macro" | Ne jamais inclure de ligne `Attribute VB_Name` dans le code envoyé via set-module ou dans un .bas déployé. Cette directive est gérée par le VBE lui-même |
| 2026-06-08 | Tentative de recréer tasks/lessons.md alors qu'il existait déjà | Toujours vérifier l'existence d'un fichier (Read ou Glob) avant de le créer. L'instruction CLAUDE.md "créer s'il n'existe pas" s'applique seulement si le fichier est absent |
| 2026-06-08 | Règle CP1252 ci-dessus partiellement incorrecte : vba_agent.py lit les .bas avec encoding="utf-8-sig" | Les .bas sur disque peuvent rester en UTF-8. La corruption survient si on crée un fichier temp CP1252 depuis un .bas UTF-8 sans conversion correcte des caractères multi-octets |
| 2026-06-08 | Attribut VB_Name dans les .bas disque provoque "Impossible d'exécuter la macro" quand déployé via set-module | Toujours filtrer la ligne `Attribute VB_Name` avant d'appeler set-module (garder VB_Name dans le .bas disque pour la documentation, le retirer dans le code envoyé au VBE) |
| 2026-06-08 | **TEXTES GARBLÉS (Ã‰ au lieu de É, â€" au lieu de —, â‚¬ au lieu de €) sur les shapes du dashboard — CAUSE RACINE DÉFINITIVE** | Ne JAMAIS convertir un .bas UTF-8 en fichier temp CP1252 avant déploiement. `vba_agent.py` lit déjà en utf-8-sig : déployer **directement** depuis le .bas UTF-8 du disque, en retirant uniquement la ligne `Attribute VB_Name`. La conversion CP1252 corrompt les caractères multi-octets LITTÉRAUX (É=0xC3 0x89, —=U+2014, €=U+20AC) présents dans les chaînes de code (ex. SetText, AddKpiCard de modDashboardGraphiques). NB : modSidebar utilise `ChrW()` pour ses accents → source ASCII pure → la conversion CP1252 y était inoffensive, d'où la confusion. **Règle : 1 seule procédure de déploiement = strip VB_Name → set-module depuis l'UTF-8, sans temp CP1252.** Après déploiement, relancer la macro de rendu (MAJ_Dashboard_Graphiques) pour réécrire les shapes. |
| 2026-06-08 | VBE affiche un buffer de code PÉRIMÉ après set-module (volet ouvert montre l'ancien code, ex. AutoNavigatePreview alors que le module déployé a AutoCollapsePreview) → "Erreur de compilation : Variable non définie" au clic sur une shape | Le set-module COM met bien à jour le CodeModule (vérifiable par inspect), mais un volet de code ouvert dans le VBE peut garder un buffer en cache et compiler à partir de lui. Fix : `save` puis fermer/rouvrir le classeur via COM (wb.Close(False) + Workbooks.Open) pour purger le buffer et forcer une recompilation propre. Vérifier ensuite via COM que le code rechargé est bien la nouvelle version. |
| 2026-06-08 | Shape-bouton (fup_btn) doit recouvrir exactement une cellule (B6) à tout niveau de zoom | Range.Left/Top/Width/Height renvoient des points (coordonnées document, invariantes au zoom). Une AddShape utilise les mêmes points. Donc `shape.Width = b6.Width` (sans marge) garantit le recouvrement exact à 100% comme à 60%. Ne pas ajouter de marge type `+30`. |

| 2026-06-09 | -2146788248 au clic Reglages/Suivi Carburant : conclu a tort "artefact COM" puis "le module compile" (j ai teste ReglageSeparateurCSV qui n utilise PAS la const fautive) | Une Private Const mal placee (apres une procedure) ne casse QUE les procedures qui REFERENCENT la const ("Variable non definie" sur son nom, en compile-on-demand), pas tout le module. Tester une fonction qui n utilise pas la const donne un faux "OK". Tester une fonction qui l utilise, ou demander a l utilisateur Debogage->Compiler VBAProject (pointe ligne+symbole exacts). |
| 2026-06-09 | Call SyncStationsVersGoogleSheets orphelin (module synchroniseGoogleForm supprime) -> -2146788248 au Worksheet_Activate de Suivi Carburant | Un Call X vers une procedure d un module SUPPRIME = erreur de COMPILATION "Sub or Function not defined", NON rattrapable par On Error Resume Next. Apres suppression d un module, grep tous les Call/OnAction/OnTime/Run vers ses procedures. |
| 2026-06-09 | Distinguer compile vs runtime via COM (Application.Run) | -2146788248 avec description VIDE (None) = erreur de COMPILATION ; avec description renseignee = erreur d EXECUTION. Discriminant fiable. Ne jamais classer une erreur COM reproductible en "artefact" sans isoler (events OFF, appel direct du helper). |
| 2026-06-09 | Boutons-image (AddPicture/AddShape) derivaient de +23 pt vers la droite quand StyleParamsPanel elargit les colonnes A:B | Toute shape/bouton-image positionnee en coordonnees ABSOLUES doit recevoir `.Placement = xlFreeFloating` (sinon defaut xlMoveAndSize -> bouge avec les cellules). La sidebar le faisait deja ; EnsurePictureButton (modGraphiques) et AddButton (modDashboardGraphiques) non -> corrige. |
| 2026-06-10 | Plan X39 figeait "Chronologie non scriptable (Add2 xlTimeline -> Err 5 en VBA) => handoff manuel". FAUX en COM. | Une **Chronologie (timeline) EST creable par COM/pywin32 late-binding** : `wb.SlicerCaches.Add2(pivot, "Date", "nom", 2)` cree le **cache** (SlicerCacheType=2), PUIS `cache.Slicers.Add(feuille, Name:=.., Caption:=.., Top, Left, Width, Height)` cree le **controle visible** (sans le 2e appel, pas de shape et TimelineState erre). Passer les args en **mots-cles** (un `Level=""` positionnel donne E_INVALIDARG 0x80070057). Pas besoin de handoff utilisateur. |
| 2026-06-10 | `SlicerCaches("tl").TimelineState.StartDate/EndDate` leve une exception en **Python COM** (artefact late-binding) mais se lit **sans erreur en VBA** | Pour lire l'etat d'une chronologie, le faire **en VBA** (`TimelineState.StartDate/EndDate`, `SetFilterDateRange d1,d2`), pas via pywin32. Ne pas conclure "API cassee" depuis une erreur COM Python : tester l'equivalent VBA (injecter un Sub diag + run). Un changement de chronologie declenche bien `Workbook_SheetPivotTableUpdate` (comme un segment). |
| 2026-06-10 | `Get-Content $f \| Measure-Object -Line` a renvoye 1431 au lieu de 1543 (fausse alerte "miroir disque perime") | Pour compter les lignes d'un fichier en PowerShell, utiliser `(Get-Content $f).Count`, PAS `Get-Content \| Measure-Object -Line` (qui compte les sauts de ligne DANS chaque element deja splitte = souvent 0). Verifier un ecart de comptage avant de conclure a une desynchronisation. |
| 2026-06-10 | Etiquette au survol des boutons via hyperlien+ScreenTip : KO total (aucune etiquette ET clic mort). Sur des shapes-image, l'hyperlien INTERCEPTE le clic (OnAction ignore) et `Worksheet_FollowHyperlink` NE se declenche PAS pour les shapes → bouton inerte. | Pour un tooltip au survol sur un bouton-image Excel : l'astuce hyperlien+ScreenTip ne marche PAS ici (casse le clic, ScreenTip non affiche). Seul l'ActiveX (`Forms.Image`/`CommandButton` + `ControlTipText` + `_Click`) donne survol+clic, mais instable + `LoadPicture` ne lit pas les PNG. Si tooltip demande : tester ActiveX en UI AVANT de livrer, sinon garder OnAction seul (sans tooltip). Toujours faire tester le clic/survol REEL par l'utilisateur (non testable en COM). |
| 2026-06-10 | Date du dernier plein affichee 06/09/2026 (6 sept) au lieu de 09/06/2026 (9 juin) dans les graphiques. `ImporterNouveauxPleins` faisait `ParseGoogleDate(CStr(cell.value))` sur une cellule `_ImportGS!Date` qui contient une VRAIE date. `CStr` → "09/06/2026" (locale FR) ; `ParseGoogleDate`, croyant recevoir du M/J/A US, lit jour<=12 comme un mois → 6 sept. Seules les NOUVELLES lignes (non encore dedupliquees) etaient touchees. | Ne JAMAIS `CStr()` une vraie date pour la re-parser : CStr est locale-dependant. Lire la date reelle : `If IsDate(cell.value) Then CDate(cell.value)` (le serial est locale-independant). L'heuristique « 1er nombre <= 12 ⇒ mois (US) » est FAUSSE pour des dates FR jj/mm/aaaa (jour <= 12). Pour ecrire une date via pywin32 sans decalage de fuseau (-2h), utiliser le **serial Excel** (`cell.Value2 = (date - 1899-12-30).days`), pas un `datetime` naif. |
| 2026-06-11 | `TimelineState.SetFilterDateRange d1, d2` sans effet (no-op silencieux) quand une borne porte une HEURE (dernier plein a 02:00) — le calage par defaut de la chronologie (`ApplyDefaultPeriodOnce`) ne « tenait » pas. | Pour caler une chronologie sur une plage, **arrondir les bornes au jour plein** (`Int(serial)`) avant `SetFilterDateRange` : une borne fractionnaire (date+heure) est ignoree. Enrober en plus la sequence de calage de `Application.EnableEvents = False` : sinon `RecreerDashboardComplet` declenche `Workbook_SheetPivotTableUpdate` → `ApplyFiltersFromControls` qui **re-applique l'ancien filtre** et annule le calage. Isoler via un helper qui lit l'etat SANS rebuild pour distinguer « SetFilterDateRange ne colle pas » de « le rebuild reverte ». |
| 2026-06-11 | Fichiers vides parasites a la racine (`code`, `$null`, `corrige`, `ui-coder`, `reconstruire`...) ramasses par `git add -A` (commit.sh) et parfois committes. `code` venait du **message de commit** contenant `design->code` : le `>code` interprete comme **redirection shell** par un hook PowerShell (PostToolUse) cree un fichier vide `code`. `$null` vient des hooks `git ... 2>$null` mal contextualises. | NE JAMAIS mettre de caracteres de redirection shell (`>`, `<`, `\|`, `&`, `2>`) dans un message de commit ou une description : ecrire « design vers code », pas « design->code ». Gitignorer les noms parasites connus. Avant `commit.sh`, faire un sweep des fichiers 0-octet a la racine (`git ls-files --others --exclude-standard` + test `! -s`) et les supprimer. Cause cote hooks : `.claude/settings.json` (Stop + PostToolUse) utilise `2>$null`/redirections — a durcir si recurrence. |
| 2026-06-12 | Bouton de rayon sélectionné (`.radius-btn.active`) illisible en thème sombre : l'override `[data-theme="dark"]` ne posait que `color:#0f172a` (texte sombre) sans overrider le `background` (resté `var(--blue-dark)` = `#0f1e30`, sombre) → texte sombre sur fond sombre, contraste **1.06:1**. | Quand on ajoute un override dark-theme `color:` sur un état actif/sélectionné, TOUJOURS overrider aussi le `background` (un texte sombre n'est lisible que sur fond clair). Vérifier le contraste WCAG des DEUX thèmes (cible ≥ 4.5:1). S'aligner sur le patron existant `.submit-btn` (fond `--blue-mid` vif + texte `#0f172a` → 7.02:1). |
| 2026-06-14 | Boutons navbar (`modSidebar`) : libellés longs (« Tableau de bord », « Réglages » actif/gras) tronqués/masqués — largeur fixe 108pt + `WordWrap=True` → le texte passait à la ligne hors du bouton (24pt de haut) → invisible. | Boutons-shapes à libellé variable : `TextFrame2.WordWrap=msoFalse` + `AutoSize=msoAutoSizeShapeToFitText`, **mesurer en GRAS** (cas le plus large : l'onglet actif met le texte en gras, sinon il déborde au survol/clic), puis repositionner selon `nav.Width` réel. **Ajouter ~24pt visuels de marge** (`+24/z`) : Excel MESURE les emojis plus étroits qu'il ne les AFFICHE (🏠 maison, 🗺 carte surtout) → sans marge, troncature de 2-3 caractères. Vérifier par export image : `Range.CopyPicture(xlScreen,xlBitmap)` → `ChartObjects.Add` → `Chart.Paste` → `Chart.Export(png)`. |
| 2026-06-14 | "Erreur à l'ouverture Excel" supposée venir du classeur projet (Workbook_Open) alors qu'elle venait d'une `Copie de AutomateExcel_AddIn.xlsm` parasite dans **XLSTART** (contenu add-in `.xlam` renommé `.xlsm` → "format ou extension non valide"). J'ai longuement instrumenté Workbook_Open pour rien avant de demander le texte exact. | Pour toute "erreur à l'ouverture Excel" : **obtenir d'abord le TEXTE EXACT du dialogue** (souvent il nomme le fichier fautif). Vérifier le dossier d'auto-démarrage `%APPDATA%\Microsoft\Excel\XLSTART` + les compléments chargés via registre (`HKCU\...\Excel\Options\OPEN=`) et `AltStartup` AVANT de suspecter le `Workbook_Open` du projet. Ne pas supposer que l'erreur concerne le fichier sur lequel on travaille. Correctif type : sortir le fichier parasite de XLSTART (move réversible), garder l'original `.xlam`. |
| 2026-06-14 | -2146788248 au clic selecteur vehicule : EnsureHeaderBand ne compilait pas. Conclu a tort "corps propre" en lisant seulement les Dim (toutes declarees) — erreur reelle = "Type argument ByRef incompatible" : `hdrTop As Single` (issu de NavbarBottom() As Single) passe ByRef a `EnsureShape(... T As Double)` ; idem `btnTop`->`EnsurePictureButton`. Introduit par le refactor Top-litteral->NavbarBottom() : un litteral 6/82 passait ByVal (OK), une VARIABLE Single passee ByRef a un param Double = erreur de COMPILATION. | En VBA, un argument passe ByRef exige une correspondance de type EXACTE : une variable `As Single` -> parametre `As Double` leve "Type argument ByRef incompatible" a la COMPILATION (compile-on-demand : ne surgit qu'a l'execution de la 1re ligne appelant la proc, jamais rattrapable par On Error). Lire les Dim ne suffit PAS : croiser le TYPE de chaque variable-argument avec la SIGNATURE du parametre. Fix : aligner le type (declarer la variable comme le param) ou forcer ByVal via parentheses `f((x))`. Localisation : Debogage->Compiler VBAProject EN PREMIER (pointe ligne+symbole, lecon #19) ; sinon tracage par checkpoints fichier (DiagStep ecrit dans %TEMP%) qui isole un compile-on-demand fault en 1 seule execution. |
| 2026-06-14 | Retrait d'un add-in charge par XLSTART (AutomateExcel_AddIn.xlam) : Move-Item echoue ("used by another process") car Excel le tient ouvert. L'add-in n'apparait PAS dans l'iteration Application.Workbooks ni dans AddIns/AddIns2 (Installed=False en chargement XLSTART) -> on croit a tort qu'il n'est pas charge. | Pour retirer un add-in XLSTART SANS fermer Excel : y acceder PAR NOM via xl.Workbooks("Nom.xlam").Close(SaveChanges:=False) (accessible meme s'il est exclu de l'iteration) pour liberer le verrou, PUIS deplacer le fichier hors de XLSTART (reversible vers Documents\Sauvegardes-XLSTART). NE JAMAIS Stop-Process EXCEL si le classeur projet est ouvert (risque de perte de donnees). Verifier les dependances AVANT tout retrait : Application.Run (refs molles), VBProject.References (refs dures), UserForms (controle date-picker) -- Suivi Conso n'utilise AUCUN des 7 add-ins XLSTART. Note : une instance Excel lancee par COM/automation (vba-agent) NE charge PAS XLSTART ; des locks ~$ horodates a la meme minute sont des orphelins. |
| 2026-06-14 | Reprise depuis `session-export-suivie85.zip` : le NOM de l'export ("e85") ne refletait PAS le projet actif (le fil reel = `suivi-conso-carburant`, classeur Excel). En plus, le script diagnostic interrompu (`Downloads\reprise-extract\check_dashboard.py`) avait ete nettoye entre sessions (dossier absent). | Pour une reprise : identifier le projet actif via les DERNIERS tours user/assistant du transcript JSONL (pas le nom du .zip). Les scripts temp de `Downloads\reprise-extract\` ne persistent pas d'une session a l'autre -> les RECREER depuis l'input `tool_use` du transcript. Toujours verifier l'etat REEL (git status, shapes/charts via COM, source .bas) avant de classer une tache "finalisee" : ici hdrBand+10 charts presents en live ET `hdrTop/btnTop As Double` en source = double preuve du fix ByRef. |
| 2026-06-15 | Reprise « commit le vba » : lancer `commit.sh` est BLOQUE par le classifieur auto-mode car `commit.sh` couple commit ET `git push origin main` (branche par defaut) — push non explicitement autorise (« commit » != « push », et la question de perimetre etait restee sans reponse). | « commit le X » = COMMITTER seulement, JAMAIS pousser sans accord explicite. Pour respecter le gate projet sans pousser : reproduire `commit.sh` a la main = aligner `package.json` (version source = `js/config.js`), `git add -A`, `git commit` (le hook husky lint-staged = eslint + vitest related sert de gate au commit), puis STOP avant `pull --rebase`/`push`. Pousser ensuite SUR ACCORD = `git push origin main` simple (re-lancer `commit.sh` sur un arbre propre sort au step 2 « aucun changement » sans rien pousser). Hygiene : separer le travail DEMANDE (feat VBA) des changements transverses traines dans l'arbre (fusion `.claude/CLAUDE.md`->`CLAUDE.md`, tooling) en commits distincts par staging selectif AVANT `commit.sh` (qui fait `git add -A` = tout). |
| 2026-06-15 | `graphify-out/graph.json` ampute de moitie (2235 -> 1092 noeuds) par un auto-`--update` lance entre deux sessions, puis committe sans verification (dans le commit VBA) : un update code-only SANS cle LLM (GEMINI/GOOGLE) ne regenere QUE les noeuds AST et ecrase les noeuds semantiques/docs. graph.html embarque son propre snapshot (n'est pas un lecteur live de graph.json) -> il affichait encore l'ancien compte. | Apres tout `graphify --update`/auto-update, VERIFIER le compte de noeuds avant de committer graph.json : `(Get-Content graphify-out/graph.json -Raw \| ConvertFrom-Json).nodes.Count` vs la version precedente (`git show HEAD:graphify-out/graph.json`). Une chute brutale = amputation. Restaurer la structure code par `graphify update . --force` (re-extraction AST des fichiers, sans LLM ~2033 noeuds) ; les ~200 noeuds semantiques restants exigent une cle LLM. `--force` ecrase MEME si moins de noeuds -> ne l'utiliser qu'avec le graph.json courant deja committe (backup git). `cluster-only` re-clusterise (gros diff graph.json) ; `update --force` re-extrait (recommande pour reparer une amputation). |
| 2026-06-16 | KPI du dashboard figes a 0 alors que les donnees existent : conclu trop vite "pas de rebuild a l'ouverture" puis "desync segments". VRAIE cause = en-tetes de la table GS_Pleins corrompus (Km/Litres/PrixL/Date renommes '0'/'2'/'3'/date) -> ColIdx("Km")=0 -> ComputeDashboardStats `If ciKm=0 Then Exit Sub` -> 0 partout. | Pour un agregat/KPI a 0 alors que les donnees existent : verifier d'ABORD le mapping colonnes (ColIdx + en-tetes de la ListObject source) AVANT de suspecter le filtre ou le declencheur de rebuild. Un `If ciX=0 Then Exit Sub` silencieux sur un header renomme donne 0 partout. Source autoritaire des bons noms = la table d'import (_ImportGS). Restaurer via `ListColumns(i).Name` (donnees intactes, reversible) et VERIFIER par un rebuild (KPI passent de 0 a des valeurs reelles). |
| 2026-06-16 | `Feuil4.Worksheet_Change` (GS_Pleins) estampillait `Modifie_local = Now()` DANS la ligne d'en-tete de la table (col 17 = timestamp). Garde-fou `Intersect(..., Rows("2:..."))` excluait la ligne 1, mais la table a son header en LIGNE 2 ($A$2:$Q$17). | Un garde-fou "ignorer l'en-tete" code en dur (`Rows("2:...")`) casse si la table n'a pas son header en ligne 1. TOUJOURS deriver la ligne d'en-tete de la table : `ws.ListObjects(1).HeaderRowRange.Row`. Sinon toute ecriture programmatique sur le header (ex. renommage de colonnes) re-declenche la corruption (le renommage lui-meme l'a re-stampe). |
| 2026-06-16 | Alerte "miroir disque vba/ STALE" (hash + nb lignes != live sur TOUS les modules) = FAUSSE alerte : diff 100% cosmetique (ligne `Attribute VB_Name` presente/absente selon fichiers, normalisation de casse par le VBE `Sh`->`sh`/`Now`->`now`/`Nz`->`nZ`, encodage UTF-8 disque vs CP1252 export COM). | Avant de conclure a une derive de code, DIFFER (`git diff --no-index disque live`) : un hash different peut n'etre que casse/encodage/Attribute. Pour editer sans risque de divergence : travailler le CodeModule LIVE (`cm.Lines`, unicode) via remplacements de sous-chaines ancres sur des LITTERAUX (stables au re-casing VBE) et non sur des identifiants. |
| 2026-06-16 | Navbar (`PoserSidebarSurFeuille`) : `x = x + wReal + gap` produisait quand meme des chevauchements en live (interaction largeur AutoSize/bold/zoom non elucidee). | Pour une rangee de shapes a largeur VARIABLE garantie sans chevauchement : faire une 2e PASSE apres creation qui repositionne chaque shape selon sa `.Width` FINALE reelle (`xx = padL: For each: nv.Left = xx: xx = xx + nv.Width + gap`). Ne pas se fier a la largeur calculee pendant la pose initiale. |
| 2026-06-16 | Boutons icone+libelle a caser dans un bandeau decale/retreci (titre+sous-titre centres occupent le milieu) : `NavbarBottom+80` fixe -> debordent sous le bandeau sur les segments. | Ancrer la rangee de boutons sur le BAS du bandeau (`ht + hH - 40`, label `ht + hH - 13`), pas sur un offset fixe depuis la navbar. Les poser en bas a GAUCHE (zone visuellement vide car titre centre). Coherence inter-modules : dash_btn (modDashboardGraphiques) et btnRecreer/Export (modGraphiques) doivent utiliser la MEME formule Y (`ws.Range("A7").Top - 42`). |
| 2026-06-16 | `excel/Suivi Conso Carburants.xlsm` (pourtant suivi par git) n'apparaissait PAS dans `git status` malgre la sauvegarde -> impossible a committer. Cause = flag git `assume-unchanged` (visible en minuscule `h` dans `git ls-files -v`, vs `H` pour les fichiers normaux). | Si un fichier SUIVI ne montre jamais ses modifications dans `git status` : verifier `git ls-files -v <file>` (lettre minuscule = assume-unchanged, `S` = skip-worktree). Lever avec `git update-index --no-assume-unchanged <file>`. Classique sur un gros binaire (.xlsm) qu'on ne veut pas committer a chaque save. Le classeur live ouvert ETAIT le fichier git-suivi (`wb.FullName`) -> `wb.Save()` suffit a le rendre committable une fois le flag leve. |
| 2026-06-16 | `/graphify --update` AMPUTE le graphe meme via le flow interactif `build_merge` (2033 -> 1685 : dedup de 348 noeuds), pas seulement via l'auto-update headless (lecon #37). La securite interne de graphify a refuse d'ecraser (« Refusing to overwrite: new graph has 1685 nodes but existing has 2033 »), MAIS un `to_json()` manuel apres coup a quand meme ecrit la version amputee. | Ne pas faire confiance a `build_merge` pour un update : il dedup agressivement. Avant tout `graphify --update` pre-commit : `cp graph.json .backup`, noter le compte de noeuds, et APRES verifier le compte ; si chute, `git checkout HEAD -- graphify-out/` (restaure graph.json + GRAPH_REPORT + html + manifest d'un coup). Ne JAMAIS appeler `to_json()` directement apres un `build_merge` qui a affiche « Refusing to overwrite ». graphify ne reconnait pas le VBA (.bas/.cls, obs #779) -> les modifs VBA ne sont de toute facon jamais dans le graphe ; le gate pre-commit n'apporte rien pour un commit VBA-only et ne doit pas committer un graphe ampute. |
| 2026-06-17 | Chirurgie OOXML retirant un externalLink (samradapps_datepicker) du .xlsm : l'avertissement "mettre a jour les liaisons" a disparu MAIS Excel a leve "probleme dans le contenu de ... recuperer le maximum" au re-open (regression signalee par l'utilisateur, capture L208). Cause = `xl/drawings/drawing6.xml` referencait le lien via `[1]` (reste de controle date-picker OLE) -> en retirant l'externalLink sans nettoyer le drawing, le dessin pointait dans le vide. Excel a ensuite "recupere" tout seul (drawing6 reecrit 7763->12308 o, `[1]` supprime) = fix non maitrise, jamais commite. | Avant de retirer un externalLink par edition OOXML, GREPPER `[N]` (N = index du lien, ex. `[1]`) dans TOUS les `xl/drawings/drawing*.xml` + leurs `_rels` ET les formules de feuilles. Si un drawing/OLE/nom defini le reference, retirer AUSSI ce controle dans la meme chirurgie (sinon corruption "probleme de contenu"). Un lien "sans formule" (LinkSources vide) n'est PAS forcement "fantome" : un controle OLE peut le pointer. Toujours faire CONFIRMER le re-open reel par l'utilisateur (texte exact du dialogue, lecon #32) AVANT de classer #fait, et committer le resultat (ne pas laisser Excel "recuperer" silencieusement un .xlsm non versionne). |
| 2026-06-17 | Déploiement VBA via script COM ad-hoc (`GetActiveObject` + `cm.DeleteLines(1,N)` puis `cm.AddFromString` sur un module ~500 l.) : HANG transitoire en pleine écriture → `modWorkbook` laissé VIDE (DeleteLines exécuté, AddFromString bloqué, process tué à ~200 s). Au 2e essai via `vba_agent set-module` (MÊME mécanisme COM) : succès → le hang était transitoire. | Écriture d'un CodeModule en COM : (1) TOUJOURS borner par timeout (Start-Job/Wait-Job) ; (2) après écriture VÉRIFIER que le module n'est pas vide (`inspect` lines>0) ; (3) préférer `vba_agent set-module/set-doc` (éprouvé) à un script `GetActiveObject` maison ; (4) `DeleteLines`+`AddFromString` n'est PAS atomique → si l'écriture hang le module reste vidé : ne JAMAIS `wb.Save` avant d'avoir re-vérifié, garder un `backup`, et le .xlsm disque non sauvé = filet (close sans save = retour disque). Encodage : `comp.Export()` écrit en CP1252 (pas UTF-8) → relire en cp1252 puis réécrire UTF-8 (direction sûre, mono-octet). Fichiers temp en mode BINAIRE (`open(...,"wb")` + CRLF explicite) : le mode texte Windows retraduit \n→\r\n et double les CRLF (\r\r\n = lignes vides fantômes). |
| 2026-06-18 | Reprise flexfuel : en câblant la MAJ globale post-plein (D1), `window.dispatchEvent(new CustomEvent('plein-added'))` a fait échouer le lint (`'CustomEvent' is not defined`, `no-undef`) car la config ESLint du projet (`eslint.config.js`) ne déclare en globals que `window`/`document`, pas les constructeurs DOM `CustomEvent`/`Event`. | Dans ce projet, dispatcher un événement custom via **`new window.CustomEvent('x', { detail })`** (convention déjà en place : `auth.js`, `router.js`, `parametres.js`, `vehicules.js`), jamais `new CustomEvent(...)` nu. Idem pour tout constructeur DOM non listé dans les globals ESLint : le préfixer par `window.`. Lancer `npx eslint <fichiers touchés>` avant de considérer une étape JS terminée (gate `--max-warnings=0`). |
| 2026-06-18 | X39 (ROADMAP) décrivait « `RefreshPrix` écrase `Prix par Station`, créer `_PrixHistoriqueJournalier` » : **périmé**. Le GAS accumulait déjà par append dans `_PrixHistory` (6 carburants) et la PQ `PrixHistory.m` le miroitait. Le VRAI bug était ailleurs : `modGraphiques.BuildPriceBlockMerged` SOURCE 2 lisait la table `PrixHistory` (format LONG Station/Date/Type/Prix) en cherchant des colonnes LARGES « E85 station »/… inexistantes → n'alimentait jamais `gPrice`. | Avant d'implémenter un item ROADMAP/spec, **vérifier l'état réel du code live** (les specs périment au fil des refactos). Pour une donnée « qui n'apparaît pas dans un graphe », tracer la chaîne complète source→lecture (ici : `_PrixHistory` GAS → `PrixHistory.m` PQ → `BuildPriceBlockMerged` VBA) et confirmer le **format réel** de la table (long vs large) avant de coder. Surfacer au lieu d'implémenter aveuglément un spec contredit par le code. |
| 2026-06-18 | Déploiement VBA via `vba-agent` : `set-module`/`inspect` (accès VBE COM) OK, mais `run --macro` échouait pour TOUTES les macros (mêmes celles non touchées) avec `-2146827284` « impossible d'exécuter la macro… macros désactivées ». Faux diagnostic tentant : « mon edit casse la compilation ». | Quand `Application.Run` échoue pour TOUTES les macros alors que `inspect`/`set-module` marchent, c'est **environnemental** (macros désactivées cette session = bandeau « Activer le contenu » non cliqué, ou Excel non-idle/cellule en édition), PAS forcément ton code. **Disambiguer par un test de revert** : redéployer le module ORIGINAL (backup) et relancer une macro pré-existante ; si elle échoue encore → environnement (ton code est bon, redéployer). La compilation ne peut alors pas être vérifiée par COM : déployer + vérifier le contenu (re-`inspect` + diff vs miroir), `save`, et déléguer la vérif visuelle à l'utilisateur (« Activer le contenu » puis bouton de reconstruction). |
| 2026-06-20 | Reprise « essencee85 » : `-2146827284` sur TOUTES les macros (cf. leçon #49 « environnemental ») avait en fait une cause RACINE de code : `vba_agent set-module` injecte la ligne d'en-tête `Attribute VB_Name = "..."` du `.bas` **dans le corps du module** (il ne la retire pas) → le projet ne compile plus → blocage de toutes les macros. | Avant de classer un `-2146827284`-toutes-macros en « environnemental », **scanner chaque CodeModule pour une ligne `Attribute ` parasite en tête** (`cm.Lines(1,1).strip().startswith("Attribute ")`) et la retirer (`DeleteLines`), puis re-tester un `run` réel. Toujours **stripper la ligne `Attribute VB_Name`** du code envoyé à `set-module` (leçons #11/#14) — `vba_agent` ne le fait pas. Parasite retiré + `save` + un `run` qui réussit = preuve que le projet compile à nouveau. |
| 2026-06-20 | Graphe `gPrice` sans marché (GAZOLE/SP95 vides) + « plus de relevés après le 16/06 » : la requête Power Query **`PrixHistory` du classeur était une COPIE de `GS_Pleins`** (lisait l'onglet `_ImportGS` des pleins, 16 col) au lieu des relevés marché `_PrixHistory` (4 col long Station/Date/Type/Prix). Le fichier disque `powerquery/PrixHistory.m` était pourtant correct → **live ≠ disque**. | Ne JAMAIS supposer que la requête PQ live == le `.m` disque. **Vérifier le schéma de sortie réel** (`ListObject` : noms + nb de colonnes via COM) et au besoin le code M live (`wb.Queries(nom).Formula`). Re-synchroniser depuis le disque : `wb.Queries(nom).Formula = open("powerquery/Nom.m", encoding="utf-8-sig").read()` puis `lo.QueryTable.Refresh(False)`. Contrôler le compte lignes/colonnes après (ici 16-col pleins → 4-col marché, 3922 lignes, dates à jour). |
| 2026-06-20 | En-têtes de la requête `GS_Pleins` corrompus (`Date`→`01/01/1970`, `Km`→`0`, `SP98 station`→`Colonne2`…) par une ligne « fantôme » (echo d'en-tête `sync_id="sync_id"`) entrée dans le GS `_ImportGS` le 17/06. La col K `Tableau2` (« Prix S98 jour ») dépend de `SP98 station` → cassée pour les pleins récents. | Corruption d'en-têtes (type leçon #38) = **ligne fantôme à la source** : la **supprimer dans le Google Sheet** (Sheets API `batchUpdate deleteDimension`, critère sûr : ligne de données dont col O `sync_id` == "sync_id") PUIS un simple `QueryTable.Refresh(False)` restaure les noms de colonnes. Vérifier les `ListColumns` après. Garder les gardes anti-réapparition : GAS `handleExport` filtre `sync_id==="sync_id"` + Excel `IsGarbageSid` (import + re-push). |
| 2026-06-20 | Suppression de la ligne fantôme via Sheets API : `403 PERMISSION_DENIED / SERVICE_DISABLED` alors que le `refresh_token` portait bien le scope `spreadsheets`. | **Scope OAuth accordé ≠ API activée** dans le projet Cloud. L'API **Google Sheets** doit être activée séparément (`console.developers.google.com/apis/api/sheets.googleapis.com`, projet du `client_id`) — l'API Apps Script l'était déjà (déploiement OK mais écriture Sheets KO). Le message d'erreur 403 donne l'`activationUrl` exacte : la transmettre à l'utilisateur (1 clic). |
| 2026-06-21 | Reprise « e85 » (= reprise de l'export « diesel ») : `npx vitest run` complet renvoie **exit 1** avec 18 « Unhandled Error » `Failed to start forks worker` / `Timeout waiting for worker to respond` (durée anormale ~434 s, machine chargée + Excel ouvert) ALORS QUE **93/93 tests PASSENT**. La session « e85 » s'était bloquée puis tuée (limite de session) en boucle sur ce run de tests sans jamais committer le fix carte déjà prêt. | Un `vitest` exit 1 dont la sortie ne montre que des **timeouts de démarrage de workers** (pool `forks`) et **0 test en échec** (« Tests N passed (N) ») est **environnemental**, pas un échec de code (cf. #49/#50). Ne PAS boucler dessus : (1) lire le bilan « Tests X passed » comme source de vérité ; (2) reconfirmer vite via `vitest related --run js/<fichiers> --no-file-parallelism` (sous-ensemble séquentiel, évite la tempête de forks) ; (3) le hook husky `vitest related` ne portant que sur les fichiers stagés, le commit passe quand même. Ne jamais relancer la suite complète en boucle pour « voir si ça passe ». |
| 2026-06-23 | Reprise « fuel » (export `session-export-fuel.zip`, session `99c4607c`, opus/xhigh/ultracode) : la session précédente a **brûlé tout son budget en exploration lecture seule** (Read/Glob/Grep/PowerShell + 1 sous-agent `Explore`) et a heurté la **limite de session** AVANT le moindre `Edit`/`Write`/`vba_agent` → **0/5 demandes traitées** (1 diagnostic partiel, 4 à faire). Les `Glob **` partis de `C:\Users\fdaub` ont aussi timeout (scan node_modules + tout le profil). | Pour statuer « traité ou non » sur un export, **3 preuves convergentes** : (1) `git status --short` propre = aucun travail en cours ; (2) scanner le transcript pour des `tool_use` d'écriture (`Edit`/`Write`/`vba_agent`) — **que des outils lecture seule = 0 livrable** ; (3) `git log --date=iso` : aucun commit dans la fenêtre horaire de la session = rien commité. Récupérer les diagnostics utiles des **sous-agents** de l'export (jsonl `subagents/`), ils ne sont pas dans le contexte perdu. **Budget** : en session lourde (opus/xhigh), ne pas tout consacrer au repérage — cibler vite (PowerShell `Get-ChildItem` au lieu de `Glob **` qui scanne node_modules/profil) et basculer en édition tôt pour ne pas mourir en phase d'exploration. |
| 2026-06-23 | Reprise « fuel2 » (= reprise de la reprise « fuel », session `94960b7e`) : fuel2 avait CODÉ le fix #1 (historique 01/01/1970) + bump version + CHANGELOG/ROADMAP/tests mais est MORTE en plein `/graphify --update` → **tout NON COMMITÉ** dans le working tree (exactement la leçon #56). En prime, fuel2 avait FUSIONNÉ par erreur 2 sections du CHANGELOG (5.21.2.0 renommée en 5.21.3.0, texte carte recollé). | Sur reprise, vérifier `git status`/`git diff` AVANT tout : un fix « déjà codé mais non commité » se **finalise** (lint + `vitest related --run --no-file-parallelism`, réparer les docs, committer) au lieu d'être refait. Toujours relire `git diff CHANGELOG.md` après une session interrompue : un mauvais merge peut fusionner/écraser une section de version → restaurer via `git show HEAD:CHANGELOG.md`. |
| 2026-06-23 | Excel 365 via COM : réécrire une formule MATRICIELLE (`AVERAGE(IF((cond)*(cond),val))`) avec `.Formula` la CASSE (résultat ""/scalaire via IFERROR) car `.Formula` applique l'intersection implicite `@` ; et `.FormulaArray` ÉCHOUE (« Impossible de définir FormulaArray ») avec des références structurées `Tableau2[...]`. | Pour (ré)écrire une formule array à structured refs en Excel 365 : utiliser **`Range.Formula2`** (préserve les tableaux dynamiques, pas de `@`). `.Formula` ne convient qu'aux formules scalaires. Après écriture, VÉRIFIER la valeur (une formule cassée renvoie "" silencieusement). Sauvegarder un backup JSON de toutes les `cell.Formula` AVANT toute chirurgie de feuille (permet un restore sélectif). |
| 2026-06-23 | Pilotage COM intensif d'Excel (Unprotect/écritures/Calculate enchaînés) : `-2147418111` « L'appel a été rejeté par l'appelé » (Excel busy) et « Range.Formula can not be set » (feuille re-verrouillée). | Excel busy en COM = transitoire : envelopper chaque appel sensible dans un **retry + pause** sur `-2147418111`/'rejected'. Déprotéger via `ws.Unprotect("")` et VÉRIFIER `ProtectContents=False` avant d'écrire. Pour exporter une image d'une feuille protégée (`CopyPicture`+`ChartObjects.Add`), **déprotéger d'abord** : `Protect DrawingObjects:=True` bloque l'ajout du ChartObject (-2146827284). |
| 2026-06-23 | ROADMAP : réutilisé par erreur `X47`/`X48` (déjà pris dans « 🔧 À faire ») pour des features livrées → collision (un même `Xnn` = 2 sujets). Corrigé en X49/X50. | Avant d'attribuer un `Xnn`, **grep `Xnn` dans TOUT le ROADMAP** (« à faire » ET « implémentées ») et prendre le prochain libre. Le ROADMAP est la mémoire du projet : une collision de numéros la rend ambiguë. |
| 2026-06-23 | `modDashboardKPI.surconso()` + `modGraphiques` lisaient la surconso E85 en `Suivi Carburant!J7`, qui contient « Conso E85 réf. (km/L) » (~15) ; la vraie surconso (%) est en `J8` (~0,23). Décalage → éco et CO2 du bandeau faussés (`essEq=litres/(1+15)`). | Avant de lire une cellule « paramètre » en VBA, VÉRIFIER son libellé/sens RÉEL dans la feuille (colonnes libellés font foi) : un réagencement (ligne ajoutée) décale les cellules sans toucher au code. Ajouter un **garde-fou de plausibilité** (surconso fraction `0<x≤1`, sinon défaut) pour qu'un futur décalage ne reproduise pas une valeur aberrante. |
| 2026-06-23 | Sélecteur véhicule sur Suivi Carburant : j'avais d'abord posé une **cellule de validation** (B3) — l'utilisateur voulait un **vrai segment (slicer)** identique à `slcVehicule`. De plus `SlicerCache.Slicers.Add(dest,…)` **IGNORE** les coords Top/Left/W/H passées (place à ~750,334 par défaut) ; `shape.Top=` peut lever -2146777998 et `slicer.Name=` être refusé par Excel. | Pour un dispositif de sélection sur une feuille, privilégier un **segment** cohérent avec l'existant (pas une cellule de validation). **Réutiliser le cache existant** pour synchroniser 2 onglets : `wb.SlicerCaches("Segment_Vehicule").Slicers.Add(autreFeuille,…)` → les 2 slicers partagent la sélection (et déclenchent `Workbook_SheetPivotTableUpdate`→`ApplyFiltersFromControls`→B5). Après `Add`, **repositionner via la shape** avec `IncrementLeft/IncrementTop` (plus robustes que `.Left=`/`.Top=`), avec retry tolérant -2146777998. Déprotéger la feuille (`Protect DrawingObjects:=True` bloque l'ajout/déplacement de shapes). |
| 2026-06-23 | `Tableau2` (Suivi Carburant) : je craignais que ce soit une requête PQ (→ colonne ajoutée écrasée au refresh). En réalité `SourceType=xlSrcRange` (tableau de **formules INDEX** miroitant `GS_Pleins`, qui est LE QueryTable PQ) ; étendu par VBA `ListRows.Add`. | Vérifier `lo.SourceType` (1=plage/formules, 3=PQ) AVANT de modifier un tableau. Sur un tableau de formules, une colonne ajoutée comme **formule de colonne du ListObject** se propage automatiquement aux nouvelles lignes, et les agrégats en **références structurées** `Tableau2[col]` suivent la hauteur variable (vérifié : éco passée de 176→192 € après un plein ajouté) → robustes. Une colonne « valeur » manuelle, elle, ne suivrait pas. |
| 2026-06-23 | Proposé au ROADMAP un « bug » X52 (nettoyage des formes dashboard `For Each…Delete` qui sauterait des éléments → doublons) sur la seule base de **doublons observés** pendant un test — SANS lire le code. En réalité `CleanupDashShapes` était **déjà** une boucle **à rebours** correcte ; et le test prouve **0 accumulation** sur 2 `RecreerDashboardComplet` (34 formes / 4 cartes stables). Les doublons venaient de MON test initial (events laissés ACTIFS → un `DebouncedRebuild` différé `OnTime` s'était superposé à mon rendu). | Avant de proposer/coder la correction d'un « bug » observé, **lire le code source réel** ET **reproduire en conditions contrôlées** (`Application.EnableEvents=False`) : un symptôme vu pendant un test instrumenté (events ON, double appel manuel) est souvent un **artefact du test**, pas un bug du code. Ne pas inscrire au ROADMAP une correction non vérifiée contre la source. |
| 2026-06-24 | Le coût du kit était lu par le code en `Suivi Carburant!B5` (vide) alors que la feuille le saisit/calcule en `B6` (libellé A6, `B12=MAX(0,B6-B11)`). `KitCost` masquait le bug par un repli `Find("kit")` non ancré qui matchait en réalité le **titre** `A2` (« …KIT ÉTHANOL ») → valeur figée au défaut 514,54 quel que soit B6. | Une cellule « paramètre » lue en VBA peut être DÉSALIGNÉE de la cellule réellement utilisée par la feuille (suite à un réagencement). La trouver via son **libellé** ET les **formules de la feuille** qui la consomment, pas via le mapping du code. Un repli `Find(motclé)` non ancré peut matcher un titre/autre libellé → l'ancrer (libellé exact + colonne) ou le supprimer dès que la cellule cible est connue. Prouver le fix par un test de sensibilité (changer la cellule → la sortie suit). |
| 2026-06-24 | Sessions interrompues/reprises à répétition : `GetActiveObject("Excel.Application")` échouait (`-2147221021`/inaccessible) alors qu'un process EXCEL existait (instance hors ROT, classeur fermé puis rouvert entre les reprises) ; et le fix X55 avait déjà été **codé sur disque** dans une passe précédente (risque de le refaire). | À la reprise : (1) `git diff` AVANT de recoder — un fix peut déjà être sur disque non commité → le **finaliser** (déployer/tester/committer), pas le refaire. (2) Excel instable : si `GetActiveObject` échoue, utiliser `win32com.Dispatch("Excel.Application")` (attache à l'instance vivante ou ouvre le classeur) + vérifier `ReadOnly`. (3) Pour savoir si un déploiement VBA a été **sauvé dans le `.xlsm`** sans ouvrir Excel : `python -m oletools.olevba --code <fichier>` puis grep le symbole attendu (ex. `MkRO`). |
| 2026-06-24 | Param « calculé » (formule) dans une synchro bidirectionnelle LWW : la surconso (J8 = formule) ne doit jamais être écrasée par un push app→Excel. | Pour une cellule-paramètre en **lecture seule** (formule) : ajouter un drapeau `cellRO` au descripteur + helper dédié (`MkRO`), garder `If … And Not cellRO Then WriteCell` côté sync, retirer le push côté Réglages, et s'appuyer sur le garde `HasFormula` existant. Le param reste **partagé en lecture** (Excel = source) sans risque d'écrasement. |
| 2026-06-25 | Déploiement COM d'un module VBA via `VBComponents.Add(1)` + `AddFromString` : Excel insère AUTOMATIQUEMENT `Option Explicit` en ligne 1 du nouveau module (option « Exiger la déclaration des variables » active). Comme le code source injecté commençait lui aussi par `Option Explicit` (après strip de la ligne `Attribute`), le module se retrouvait avec **deux `Option Explicit`** → erreur de compilation → **toutes** les macros du projet « indisponibles » (`SuiviVehicule_EnsureBoutons` injoignable, code -2146827284). | À l'injection COM d'un `.bas` : après `VBComponents.Add`, **vider le module** (`CodeModule.DeleteLines(1, CountOfLines)`) AVANT `AddFromString`, pour ne pas dupliquer le `Option Explicit` auto-inséré. Vérifier les 3 premières lignes après injection. Une seule erreur de compilation dans n'importe quel module rend TOUT le projet VBA non exécutable. |
| 2026-06-27 | Reprise « fuel2 » #24 : tenté d'embarquer une carte interactive dans l'onglet via le contrôle WebBrowser ActiveX (`Shell.Explorer.2`) + IE11. Échec `1004 / -2146827284 « Impossible d'insérer un objet »`. Longue chasse au réglage (Trust Center, `DisableAllActiveX`, redémarrage process) AVANT d'isoler que le contrôle lui-même est mort. | Sur **Windows 11 + Office récent**, le moteur **IE/MSHTML est retiré → le contrôle WebBrowser `Shell.Explorer.2` n'existe plus** : `OLEObjects.Add` échoue quoi qu'on fasse côté ActiveX. **Diagnostic décisif en 1 test** : insérer un ActiveX standard `Forms.CommandButton.1` (OK = ActiveX activé) vs `Shell.Explorer.2` (échec = contrôle retiré). Si le standard passe et le WebBrowser non → composant absent, **abandonner l'embarqué** (pas un réglage). Seule voie interactive : ouvrir le HTML Leaflet dans le navigateur (`Shell explorer.exe`). Ne PAS modifier soi-même `DisableAllActiveX` (réglage de sécurité) — c'est à l'utilisateur ; et `FEATURE_BROWSER_EMULATION` est inutile si le contrôle n'existe pas. |
| 2026-06-27 | `mcp__computer-use__open_application "Excel"` à répétition pour « ramener Excel au premier plan » → **crée un nouveau classeur vide à chaque appel** (Classeur1/2/3…), pollue l'instance et brouille le diagnostic (l'utilisateur l'a signalé). | Pour amener un classeur DÉJÀ ouvert au premier plan, **ne pas** ré-`open_application` (relance Excel + classeur vierge) : activer la fenêtre via COM (`wb.Activate`/`Window.Activate` + `Application.Visible=True`) ou simplement **screenshot du premier plan courant**. `open_application` = lancer une app, pas « focus » une fenêtre existante. |
| 2026-06-27 | Reprise « fuel3 » : `commit.sh` ANNULÉ (`❌ Commit annulé`) par `npm test` complet — 4 « Failed to start forks worker » (timeouts, 375 s, Excel ouvert) ALORS QUE **228/228 tests passent** (leçon #54 reconfirmée). En prime, le pipe `... | tail` masquait l'échec (exit 0 du tail ≠ exit de commit.sh). | Un échec `commit.sh` dont la sortie ne montre que des **timeouts de workers forks** + **0 test échoué** est environnemental : committer **directement** via `git add -A && git commit` (le **hook husky lint-staged ne teste que les fichiers js stagés** → charge bien moindre, passe). Ne JAMAIS conclure « commit OK » sur l'exit code d'un pipe `| tail`/`| head` (il reflète le dernier process du pipe) : vérifier `git log`/`git status` après. |
| 2026-06-27 | Reprise « fuel3 » : `vba_agent set-module` sur `modCarte` (~1840 l.) a HANGÉ >1 min **parce que l'éditeur VBA (VBE) était OUVERT** (vu dans les aperçus barre des tâches). Tuer le python pendant le hang a laissé un **dialogue modal dans le VBE** → tout le COM Excel bloqué (même `GetActiveObject` timeout). La fenêtre VBE refusait de se restaurer (clic barre des tâches/Alt+Tab via computer-use) → fallu demander à l'utilisateur de fermer le dialogue. | Avant tout `set-module`/`run` COM : **s'assurer que le VBE est FERMÉ** (un VBE ouvert sur le module cible bloque `CodeModule.DeleteLines`/`AddFromString`). Lancer le déploiement **en arrière-plan borné** (leçon #46) ; si hang, **ne pas tuer brutalement** sans savoir que ça laissera un modal VBE. Un modal VBE bloque 100% du COM (GetActiveObject inclus). Restaurer une fenêtre minimisée avec modal via computer-use est peu fiable (clic miniature/Alt+Tab échouent) → demander à l'utilisateur de cliquer le dialogue. NE PAS `taskkill EXCEL.EXE` (perte de données, leçon #34). |
| 2026-06-27 | Reprise « fuel4 » #268 : cause RACINE de « trop zoomé » trouvée tardivement = `.b-pin` est un conteneur **flex**, l'`<img>` flex-item a `min-width:auto` par défaut → `width:100%`+`object-fit:contain` IGNORÉS, l'image prend sa largeur intrinsèque, **déborde** et est **rognée** par `overflow:hidden` (« Intermarché »→« ermarc »). J'ai perdu des heures à redimensionner (compact/hug/plafond) ET à vérifier avec le **Chromium intégré de Playwright** qui NE reproduisait PAS le bug — alors que le **Chrome installé** de l'utilisateur le montrait. | Pour un rendu HTML/CSS livré dans le navigateur de l'utilisateur, VÉRIFIER avec son **vrai navigateur** : `chromium.launch({channel:'chrome'})` (ou `msedge`), PAS le Chromium bundle de Playwright (différences de rendu réelles). Mesurer `img.getBoundingClientRect().width` vs `.b-pin` pour détecter un **débordement** (imgW>pinW = rognage). Sur tout `<img>`/`object-fit` enfant d'un conteneur **flex/grid**, ajouter **`min-width:0;min-height:0`** (le défaut `min-width:auto` empêche la contraction → débordement). Un symptôme « zoomé/coupé » d'image = souvent un débordement clippé, pas une taille. Demander une CAPTURE de l'écran réel tôt quand on ne reproduit pas. |
| 2026-06-27 | Reprise « fuel4 » #268 (« icônes marqueurs trop zoomées ») : 1er fix (v5.22.5.0) = élargir les pastilles wordmark jusqu'à 84 px → réglait la lisibilité mais rendait les marqueurs **trop GROS** → l'utilisateur a re-signalé « encore trop zoomés » (= trop gros, PAS rogné). J'avais aussi mal interprété « zoomé » au départ (supposé rognage/viewBox manquant) avant de mesurer. | « zoomé » est ambigu : peut vouloir dire **trop gros** (footprint) et non rogné. AVANT de coder : (1) **reproduire visuellement** le rendu réel (ici planche Playwright + mesure `getBBox()` vs `viewBox` → a infirmé l'hypothèse rognage), (2) **confirmer le sens** du symptôme avec l'utilisateur, (3) pour un changement de **taille/design**, montrer un **comparatif de plusieurs tailles** et faire choisir AVANT de déployer (évite l'aller-retour). Un fix de lisibilité qui agrandit peut déborder en « trop gros ». Vérifier le HTML généré (`%TEMP%\suivi_carte*.html`) porte bien le fix avant de conclure (live vs périmé). |
| 2026-06-27 | Reprise « fuel3 » #18 (« cartes Google Maps au lieu de Leaflet ») : la clé `GOOGLE_MAPS_API_KEY` de `config.js` est **restreinte par référent HTTP** (`fdaubercy.github.io`) → testé empiriquement en `file://` (page test + Edge + screenshot) = **`gm_authFailure`** « Google Maps ne s'est pas chargé correctement ». Les cartes Excel s'ouvrant en `file://` (leçon #68), la Maps JS API NE peut PAS utiliser cette clé. | Pour Google Maps JS dans un HTML local `file://` : il faut une clé **sans restriction référent** (restreinte par API Maps JavaScript + quota). Comme le dépôt est **public**, NE PAS mettre cette clé dans `js/config.js` → la stocker **hors dépôt** dans le classeur (propriété de script, pattern `SYNC_SECRET`) et prévoir un **repli OSM/Leaflet** automatique (`gm_authFailure`/clé absente). Tester la compatibilité `file://` d'une clé AVANT de coder le rendu (page test minimale + `window.gm_authFailure` + ouverture navigateur réelle), ne pas supposer. |
| 2026-06-28 | Reprise « fuel5 » #18/#5 (carte Google Maps locale) : après `vba_agent set-module` sur `modCarte` (depuis le `.bas` disque), `run CarteProximite` a échoué en `-2146827284` « macro non disponible / macros désactivées » = **projet ne compile plus** → re-déclenchement EXACT de la leçon #54 : `set-module` (DeleteLines+AddFromString) **réinjecte la ligne `Attribute VB_Name = "modCarte"`** (ligne 1 du `.bas`) DANS le corps du module → erreur de compilation bloquant TOUTES les macros. | Avant tout `set-module`, **stripper la 1ʳᵉ ligne `Attribute VB_Name = "..."`** du code envoyé (le `.bas` disque DOIT la garder, c'est le format canonique ; c'est l'injection qui ne doit pas la propager). Diag décisif d'un `-2146827284`-toutes-macros : forcer **Debug→Compile** via COM (`app.VBE.CommandBars.FindControl(Type=1, Id=578).Execute()`) puis lire `ActiveCodePane.GetSelection()[0]` → la ligne fautive ; si c'est `Attribute VB_Name` en tête, `CodeModule.DeleteLines(1,1)` + recompiler. Toujours faire un `run` réel d'une macro APRÈS `set-module` pour prouver que le projet compile (le `set-module ok:true` ne prouve PAS la compilation). |
| 2026-06-28 | Reprise « fuel8 » (reprise de fuel6) : le deploiement GAS **prod** (`node gas-deploy.mjs`) a ete REFUSE par le classifieur auto-mode (« Production Deploy … resume-session required presenting a plan and awaiting validation »), MEME si les decisions produit G2 avaient ete pre-validees en seance et que l'utilisateur avait dit « attaque g2 ». | En reprise (et en general), un **deploiement en production** (GAS prod, push live, etc.) exige une **autorisation explicite par deploiement**, distincte de la validation du design. Procedure : (1) coder + verifier la syntaxe (`node --check` sur une copie `.js` du `.gs`, l'extension `.gs` n'est pas reconnue par node), (2) **committer la source** (durable, sans effet prod), (3) DEMANDER l'autorisation de deployer, puis lancer `gas-deploy.mjs`. Ne pas contourner le refus du classifieur. |
| 2026-06-28 | Reprise « fuel8 » : `graphify --update` (flow `build_merge`) sur des commits docs+code (CHANGELOG/ROADMAP + config.js + .bas/.gs) a CORRECTEMENT augmente le graphe a chaque passe (2046→2080→2100→2124, +20-34 noeuds) SANS amputation, contrairement aux craintes des lecons #37/#44. Cle = fournir au `build_merge` une **extraction fraiche incluant les docs changes** (sous-agent semantique sur CHANGELOG/ROADMAP) ; le merge ajoute alors les nouveaux concepts au lieu de dedupliquer a la baisse. Note : `.bas` ET `.gs` sont **ignores par graphify** (detect ne les voit pas) → un commit VBA/GAS-only ne change le graphe que par ses docs. | Pour un `--update` pre-commit sain : toujours passer le(s) doc(s) modifie(s) au sous-agent semantique (pas seulement l'AST), garder la garde anti-amputation (backup + compte avant/apres), et reutiliser `.graphify_labels.json` par id de communaute pour ne pas regresser les 300 noms en « Communaute N ». L'amputation des lecons #37/#44 vient d'un `--update`/`--force` SANS extraction des docs (code-only ou cle LLM absente), pas du `build_merge` en soi. |
| 2026-06-28 | Reprise « fuel6 » : `vba_agent set-module --code-file` a echoue avec `[WinError 3] ... \modCarte_body.bas\gen_py` quand le `--code-file` etait dans le scratchpad profond (`AppData\Local\Temp\claude\<projet>\<uuid>\scratchpad\...`). Le cache `gen_py` de win32com a derive vers un chemin invalide a cause de la longueur/profondeur du chemin. | Pour `vba_agent set-module`, ecrire le fichier `--code-file` dans un chemin COURT (ex. `C:\Users\fdaub\AppData\Local\Temp\mc_body.bas`), pas dans le scratchpad imbrique. Si `[WinError 3] ...gen_py`, relancer avec un chemin temp court avant de chercher plus loin. |
| 2026-06-28 | Reprise « fuel9 » (reprise en chaine de fuel8/fuel6) : la session a coupe (limite) PILE apres `gas-deploy` v57 reussi mais AVANT d'executer la coloration de l'historique. Le script `g2color.py` avait ete ecrit dans un chemin temp STABLE (`%TEMP%\g2color.py`) et l'autorisation utilisateur « deployer + colorer maintenant » etait deja donnee → la reprise n'a eu qu'a relancer le script (5911/5911 lignes colorees). | Quand une action prod multi-etapes risque d'etre coupee, ecrire l'etape restante comme un SCRIPT autonome dans un chemin temp stable (pas le scratchpad imbrique, leçon WinError 3) AVANT de l'executer : si la session coupe, la reprise se limite a `python <script>` sans re-deriver le contexte. Pour une coloration Sheets via API (scope `spreadsheets`), repliquer `appliquerMFCPrix` en `updateCells`/`batchUpdate` est idempotent → re-executer sans risque. Verifier la reussite par la reponse API (`colored: N/N`), pas par une inspection visuelle. |
| 2026-06-28 | Reprise « fuel10 » (reprise en chaine de fuel9) : la session a coupe (limite hebdo) en plein milieu de la SEULE action restante = fournir la procedure de rotation du secret OAuth, suite a une alerte securite heritee de fuel9/fuel10 affirmant que « .claude/gas-config.json (secret OAuth + refresh_token) a transite dans l'historique du depot public ». VERIFICATION en reprise : `git log --all -- .claude/gas-config.json` montre 3 commits (04fcb68 vide / 70becf3 placeholders « ⚠️ À renseigner » / 90b395a retrait), et `git grep "GOCSPX-"` + `git grep "1//04"` sur `git rev-list --all` ne retournent RIEN → les VRAIS secrets n'ont JAMAIS ete commites ; seuls des placeholders ont transite ; le fichier etait `.gitignore` (ligne 24) AVANT la saisie des vrais secrets. L'alerte etait un FAUX POSITIF. | Avant de declarer une fuite de secret « dans l'historique public » et d'exiger une rotation, PROUVER que les VRAIES valeurs (pas des placeholders) sont presentes : `git grep -I -l "<prefixe-reel>" $(git rev-list --all)` pour le pattern reel (GOCSPX-, 1//, AKIA, etc.), pas seulement `git log -- <fichier>` qui montre l'existence du fichier sans son contenu. Un fichier gitignore aujourd'hui a pu n'avoir jamais contenu que des placeholders. Ne pas propager une alerte securite d'une session a l'autre sans la re-verifier sur les valeurs reelles. |
| 2026-06-28 | Reprise « fuel10 » suite : rotation OAuth de `.claude/gas-config.json` (gitignore) demandee par l'utilisateur (defense-in-depth, AUCUNE fuite reelle prouvee). Sequence d'erreurs traversee avant succes : (1) `redirect_uri_mismatch` au Playground = `https://developers.google.com/oauthplayground` absent des « URI de redirection autorises » du client OAuth en Console ; (2) `invalid_client / The provided client secret is invalid` = client_secret copie ne correspondait pas au client_id (mauvaise ligne / mauvais client en Console) ; (3) `unauthorized_client / Unauthorized` = refresh_token emis par le client PAR DEFAUT du Playground car la case « Use your own OAuth credentials » s'etait DECOCHEE au rechargement de page. Succes apres recopie du bon secret + regeneration du refresh_token avec la case cochee. Validation : `node gas-deploy.mjs --check` → « Access token obtenu (refresh_token OK) ». | Diagnostic OAuth Google par le message d'erreur : `redirect_uri_mismatch`→ajouter l'URI exacte du Playground en Console (sans slash final) ; `invalid_client`→paire client_id↔client_secret fausse (NE concerne PAS le refresh_token), recopier le secret du BON client ; `unauthorized_client`→le refresh_token n'appartient pas a ce client, le regenerer en gardant « Use your own OAuth credentials » COCHEE jusqu'a l'echange (ne pas recharger la page entre-temps). Mettre a jour le fichier via un `json.load/json.dump` cible (champs oauth.* uniquement, ensure_ascii=False, indent=2) sans afficher les valeurs, puis valider par `--check` (jamais par inspection visuelle). Consent screen doit etre « In production » sinon refresh_token expire a 7 jours. |
| 2026-06-29 | X41 (injection VBA) : `vba_agent set-doc` sur le doc-module `Feuil2` a TIMEOUT a 2 min (Excel occupe : un rebuild `OnTime`/evenement en cours). L'ecriture half-faite a CORROMPU le module → il ne restait que 87 car. (le tail orphelin `...Select / End Sub` d'`AjouterLignePlein`), produisant un `End Sub` sans en-tete → projet NON COMPILABLE → toutes les macros echouent en `-2146827284`. Piege secondaire : tester la presence d'une garde via `'On Error Resume Next' in source` = FAUX POSITIF (cette chaine existe ailleurs, ici dans `Worksheet_Change`). | Pour `set-doc`/`set-module` : (1) donner un timeout LARGE (>=240 s) car Excel peut etre occupe (debounce rebuild) ; (2) APRES toute ecriture, RE-INSPECTER et verifier l'INTEGRITE complete (toutes les procedures attendues presentes + longueur plausible), pas seulement `ok:true` ni un `in source` ambigu ; (3) si timeout, considerer le module potentiellement CORROMPU et re-injecter la source complete depuis le fichier `--code-file` (garder ce fichier + le `backup` horodate de debut de session) ; (4) verifier une garde par le BLOC exact (regex `Sub X.*?End Sub`), jamais par une sous-chaine globale ; (5) prouver la compilation par un `run` d'une fonction publique sans effet de bord (ex. `GraphAutoActif`) APRES restauration. |
| 2026-06-29 | commit.sh a ete TUE 2x par timeout (outil Bash plafonne a 120s ; un timeout interne 300s coupait aussi commit.sh AVANT le git add) -> aucun commit, exit 0 trompeur. Le commit.sh complet (lint + vitest + add + commit + pull --rebase + push) prend ~410s sur ce poste. | Lancer commit.sh en ARRIERE-PLAN REEL (run_in_background, SANS timeout interne) et attendre la notification de fin, puis verifier `git log -1` + `git status`. Ne jamais conclure au succes sur un exit 0 d'un wrapper : verifier que HEAD a avance. |
| 2026-06-29 | En cours de session, `which python` a DERIVE de Python313 (avec pywin32) vers Python312 (SANS pywin32) -> `vba_agent set-module` echouait en `No module named win32com` alors que les COM marchaient plus tot. Cause probable : reinit du shell depuis le profil entre 2 appels (npm/commit.sh). | Pour TOUT appel COM/pywin32 (vba_agent, scripts win32com), invoquer l'interpreteur par CHEMIN COMPLET `C:\Users\fdaub\AppData\Local\Programs\Python\Python313\python.exe` (= contenu de `graphify-out/.graphify_python`), jamais `python` nu. Eviter `bash -c "... python ..."` imbrique (resout un python different). |
| 2026-06-29 | X43c (rebuild incremental) : implemente en HYBRIDE (signature d'etat + classification rsNone/rsTargeted/rsFull dans modFiltres.DebouncedRebuild). Decision assumee : rsTargeted REPLIE sur rebuild complet pour ce 1er increment (le ciblage par bloc exigeait un refactor risque de BuildAggregates, monolithe 240 lignes ; et seuls carburant/budget/CO2/annee en beneficieraient, vehicule/periode etant incompressibles). Le vrai gain livre = no-op skip (rsNone). Optimisation par bloc deferree -> ROADMAP X43c-opt. | Sur un dashboard reactif mature, mesurer OU l'incremental paie AVANT de refactorer : si la majorite des filtres invalident tout (vehicule/periode), le no-op skip rapporte plus que le ciblage par serie, pour un risque bien moindre. Tester DebouncedRebuild via COM en pilotant B5/B6 + horodatage B8 (events OFF) : no-op => B8 inchange + statusbar 'deja a jour' ; changement => B8 avance. |
| 2026-06-29 | Sur 4 commits (X41/X51 v5.27, W79 v5.28, X43c v5.29), j'ai bumpe package.json (via commit.sh) mais OUBLIE de mettre a jour `APP_VERSION` dans `js/config.js` -> reste fige a 5.26.0.0 (version affichee dans l'app #appVersion fausse). commit.sh AVERTIT de la divergence (etape 3 « Divergence : APP_VERSION (X) != message (Y) ») mais ne BLOQUE pas (warning), et je n'ai pas relu ses warnings. | `js/config.js` `APP_VERSION` = **source de verite de la version, a editer A LA MAIN** dans CHAQUE commit feature/fix (commit.sh n'aligne que package.json, il ne touche PAS config.js). Avant `commit.sh`, mettre a jour APP_VERSION = la version du message. Relire les warnings de commit.sh (etape 3) : un « Divergence APP_VERSION » signale un oubli. Le job CI `version-check` compare config.js au dernier tag. |
| 2026-06-30 | RECIDIVE de l'oubli APP_VERSION : sur le fix sync hors-ligne v5.29.0.1, j'ai a nouveau commit (13af697) sans aligner `APP_VERSION` dans `js/config.js` (reste a 5.29.0.0) -> 2e commit correctif (862578e) necessaire. La lecon du 2026-06-29 existait deja dans ce fichier mais n'a PAS ete appliquee : la session avait demarre directement sur une demande utilisateur, sans le rituel projet (lecture de lessons.md). | Promu en MEMOIRE NATIVE (`version-app-config-js-manuelle.md`, injectee a chaque demarrage) pour ne plus dependre de la lecture de lessons.md. Regle operationnelle : faire de l'edition de `APP_VERSION` (js/config.js L2) = la version `[vX.Y.Z.W]` du message une etape OBLIGATOIRE du workflow de commit, AVANT `./commit.sh`, au meme titre que CHANGELOG/ROADMAP. commit.sh n'aligne que package.json (warning non bloquant). |
| 2026-06-30 | Bug PWA figee : j'ai d'abord suppose un probleme de TIMING du hook `closeBundle` sous rolldown-vite (existsSync -> return) et perdu 2 itterations de build a tester cette piste, alors que la vraie cause etait que le plugin `swVersionPlugin()` etait DEFINI mais jamais ajoute a `plugins: []` dans vite.config.js (donc il ne tournait jamais, ni dev ni build). | Diagnostic plugin/outil de build qui « ne fait rien » : verifier D'ABORD que le plugin est bien ENREGISTRE (`grep "plugins"` dans la config + chercher l'appel `monPlugin()`) AVANT d'echafauder des hypotheses sur l'ordre/timing des hooks. Un plugin Vite defini mais absent de `plugins: []` = code mort silencieux, aucune erreur. |
| 2026-06-30 | Bug « clic badge hors-ligne = rien ne se passe » : j'ai d'abord conclu (a tort) que c'etait un cache SW perime (fix W82 v5.29.0.2). Le vrai bug etait distinct et seule la REPRODUCTION AU NAVIGATEUR l'a revele : `#feedback` (cible de showFeedback) vivait DANS `view-saisie`, masquee en display:none des que l'app est sur une autre vue (demarrage sur view-accueil) -> le message s'ecrivait dans un conteneur invisible. Un element de feedback GLOBAL (declenche par un controle global = badge header) ne doit jamais vivre dans une vue scoped. | Pour un symptome « rien ne se passe » sur une UI multi-vues : REPRODUIRE au navigateur (preview_eval : cliquer + lire feedbackText + getBoundingClientRect + closest('.view')) AVANT de conclure. Un element cible de feedback doit etre HORS des vues (enfant du shell global) si son declencheur est global. Verifier `offsetParent` est trompeur pour position:fixed (toujours null) -> utiliser getBoundingClientRect + display calcule. |
| 2026-06-30 | Panne « 📵 Toujours hors-ligne » : l'app recevait du HTML 403 « Acces refuse » au lieu de JSON depuis l'endpoint GAS -> `r.json()` levait -> chemin offline. Cause : le deploiement Web App VERSIONNE utilise par config.js (v57, AKfycbwI) avait son autorisation d'execution `executeAs:USER_DEPLOYING` INVALIDEE par la rotation OAuth du 28/06. Un redeploiement VIA L'API (PUT version v58, meme manifeste ANYONE_ANONYMOUS) n'a PAS reactive l'acces (toujours 403). Seul le deploiement HEAD (re-autorise en ouvrant l'editeur le 28/06) repondait 200. | Apres une rotation/regeneration des identifiants OAuth d'un projet GAS : les deploiements Web App versionnes existants peuvent tomber en 403 et l'API REST ne peut PAS les re-autoriser (le consentement `executeAs` est interactif). Fix = l'utilisateur redeploie dans l'EDITEUR (Deployer > Gerer les deploiements > crayon > Version: Nouvelle version > Deployer, puis aller AU BOUT du consentement) -> URL /exec inchangee. Diagnostic rapide : `curl -sL <GAS_URL>` ; HTML <title>Acces refuse</title> = probleme de deploiement, PAS l'app/le reseau. POST GAS en curl renvoie un 302 vers googleusercontent.com/macros/echo (normal) ; suivre avec un GET (pas un re-POST, sinon 405/411). REQUIRE_AUTH=1 -> sans idToken Google valide, reponse JSON {success:false,error:unauthorized,401} = connexion OK, juste pas d'identite. |
| 2026-06-30 | RECURRENT : en injectant un module via `vba_agent set-module --code-file`, j'ai laisse la ligne d'en-tete `Attribute VB_Name = "modXxx"` dans le code-file -> elle apparait en TEXTE LITTERAL (parasite) dans le module injecte, car vba_agent fixe deja le nom via --name. | AVANT tout `set-module`/`import` via vba_agent : STRIPPER du code-file la/les ligne(s) `Attribute VB_*` (notamment `Attribute VB_Name = "..."`). Le code-file doit commencer directement par `Option Explicit` (ou la 1re vraie ligne). Le nom du module vient EXCLUSIVEMENT de l'option --name. Vaut aussi pour le nettoyage : retirer les modules de diagnostic temporaires (ex. modDiagFS) en fin d'intervention. |
| 2026-06-30 | `vba_agent inspect` (dump source) a plante en silence sur `'charmap' codec can't encode '\x8f'` (sortie Windows cp1252) -> JSON corrompu lu comme {components:0}, ce qui m'a fait conclure A TORT que le VBA vivant avait diverge (macro absente). | Pour `vba_agent inspect`/tout dump VBA volumineux : forcer `PYTHONUTF8=1 PYTHONIOENCODING=utf-8` et rediriger `2>/dev/null` (pas `2>&1` qui melange l'erreur au JSON). TOUJOURS verifier `d['ok']` avant d'exploiter la sortie ; `components:0` = suspecter un plantage d'encodage, pas une absence reelle. Diag « aucune macro ne s'execute » alors que VBAWarnings=1 + refs OK + pas de doublon Public : suspecter projet VBA en mode break/design (planté a l'ouverture) -> fermer/rouvrir Excel avant de chercher plus loin. |
| 2026-06-30 | Ouvrir le classeur via COM `Workbooks.Open` (automation) le charge SANS activer les macros -> `Application.Run` echoue ('macro indisponible/desactivee') alors que les memes macros tournent quand l'utilisateur ouvre NORMALEMENT (double-clic). M'a fait croire a tort a un blocage persistant. | Pour TESTER une macro/un formulaire VBA, exiger une instance ouverte NORMALEMENT par l'utilisateur (macros actives) ; une instance ouverte par automation sert a EDITER/injecter (set-module, shapes, save) mais pas a executer. L'edition VBProject (AccessVBOM) et les ops shapes/Save marchent meme en automation ; seul Application.Run est bloque. Ne jamais livrer/committer du VBA non execute-teste dans une instance normale. |
| 2026-06-30 | Module `modSuppression` importe DEUX FOIS -> non seulement les Sub/Function dupliques (SupprimerUnPlein) mais aussi le BLOC DE DECLARATIONS en tete (Private Const FORM_NAME/WS_GS/COL_*) + Option Explicit duplique -> erreurs « Nom ambigu » en cascade (une corrigee, la suivante apparait). Mon 1er dedup (coupe au 2e Sub) n'a traite que les procedures, pas l'en-tete. | Dedoublonner un module double-importe = reconstruire ENTIEREMENT : 1 en-tete + 1 `Option Explicit` (tout en haut) + 1 bloc de Const + 1 copie de chaque proc. Le check anti-doublon doit couvrir AUSSI les declarations de module (`Public`/`Const`/`Global`), pas seulement Sub/Function. Verifier x1 pour CHAQUE Const ET chaque proc avant injection, puis controle GLOBAL projet (0 doublon interne + 0 conflit Public). Cause profonde : module jamais committe au repo -> re-importable en double ; LE VERSIONNER. |
| 2026-07-01 | Reprise « fuel1 » (export session `2877b95f`, opus/medium/auto) : session MORTE sur limite HEBDO (reset 11:10) qui a coupe 3 sous-agents (W78/X40/S13) ET le fil principal AVANT toute revue/test/commit -> 7 fichiers CODES mais NON COMMITES. Un sous-agent (W82) avait pose l'echafaudage (const `VISIBILITY_RETRY_MIN_MS` + `_lastSyncAttemptTs`) mais N'AVAIT PAS cable la garde dans le handler `visibilitychange` -> code MORT (eslint `no-unused-vars`), tache en apparence « completed » mais fonctionnellement 🔶 incomplete. | Sur reprise, ne pas se fier au statut « completed » d'une task-notification d'un sous-agent coupe par limite : VERIFIER l'integrite fonctionnelle du travail sur disque. `eslint` (`no-unused-vars`) est un detecteur fiable d'echafaudage laisse a moitie (const/var declaree mais jamais lue = feature non finie). Toujours relancer lint + build + test sur le working tree AVANT de committer un travail herite d'un agent interrompu ; finaliser (cf. #56/#24) au lieu de refaire. |
| 2026-07-01 | X40 (linter VBA `check_vba_compile.py`) livre par un sous-agent : a la 1re execution, 9 « violations » dont 5 FAUX POSITIFS du linter lui-meme -> (a) il ne retirait pas le prefixe `Module.` des cibles `.OnAction` qualifiees (`modFuelPanel.ToggleFuelPanel` cherche tel quel dans un set de noms nus) ; (b) il flagait une cible CONCATENEE dynamique (`"modSidebar.NavSidebar_" & k`, resolue en NavSidebar_0..5 a l'execution). Les 4 autres venaient de `General.bas`, module VIVANT dans le .xlsm mais NON VERSIONNE dans `vba/`. | Un linter de refs VBA doit : retirer les qualifications `Classeur!`/`Module.` avant de tester l'existence (nom terminal only) ET ignorer toute cible non-litterale (concatenation `&`, variable) car invérifiable en statique. Avant de cabler un tel linter en gate bloquant, s'assurer que TOUS les modules cibles d'appel sont versionnes : un module live-only (cf. `vba-module-non-versionne`) genere des faux positifs a chaque commit. Extraire le module manquant du .xlsm en LECTURE SEULE (`olevba --code`, UTF-8) et le versionner resout la cause racine sans injection COM. |
| 2026-07-01 | X47 (ROADMAP « eco E85/CO2 negatives si ref SP98 manque ») visait `ComputeDashboardStats.eco`/`ComputeKPIs.outEco` : ces fonctions sont devenues du CODE MORT (calcule mais plus affiche - la carte KPI a ete remplacee par « Rentabilite kit », l'eco affichee vient d'une colonne FORMULE `Tableau2` + graphe X9). Implementer X47 tel quel = corriger du code mort, zero effet visible. La moitie « CO2 negatif » etait deja corrigee (bug surconso J7/J8). | Avant de coder un item ROADMAP qui NOMME une fonction precise, VERIFIER que cette fonction est encore sur le chemin d'affichage reel (un refactor a pu la rendre morte) : (1) grep les consommateurs de sa sortie (`.eco`/`outEco` -> assignes mais jamais lus = morts) ; (2) lire la valeur reellement affichee dans le classeur LIVE (ici colonne Tableau2 « Economie cumulee » = 0 negative sur 18) avant de conclure a un bug. Un ROADMAP ecrit en vN peut decrire un chemin decouple depuis. Cloturer l'item comme obsolete AVEC preuve plutot que de livrer un no-op ; proposer le nettoyage du code mort en item distinct. |
| 2026-07-01 | X44 Phase 1 (injection COM de 4 modules VBA) : `vba_agent set-module` puis `CodeModule.AddFromString` ont HANGE (timeout 2 min) DES le 1er module (modSyncJson cree mais vide, 2 lignes) — sans modal VBE (COM restait `Ready=True`, VBE ferme). Cause probable : `AddFromString` sur un projet volumineux/occupe se bloque de facon transitoire. | Si `set-module`/`AddFromString` hangue sans modal, basculer sur `VBComponents.Import(path)` : chemin de code COM DIFFERENT, robuste, non bloquant. Import CONSOMME la ligne `Attribute VB_Name` du .bas pour NOMMER le composant (donc utiliser le `.bas` CANONIQUE non strippe, contrairement a set-module/AddFromString qui exigent le strip). Recette de remplacement : `proj.VBComponents.Remove(c)` (si existe) puis `proj.VBComponents.Import(vba/Mod.bas)`. Verifier `CountOfLines` + `Lines(1,1)` = banniere (pas d'`Attribute` parasite) apres import. |
| 2026-07-01 | X44 Phase 1 (decoupe d'un monolithe VBA) : le design herite (fuel2) listait `RowToJson` et `ToNum` parmi les helpers « purs » a rendre Public dans un module extrait. AUDIT : `RowToJson` lit `COL_MODIFIED` (etat module) = NON pure ; `ToNum` entre en COLLISION avec `modSaisie.ToNum` deja `Public` (signature differente) = « Nom ambigu » a la compilation si rendu Public. | Avant d'extraire des fonctions VBA vers un module et de les passer `Public` : (1) verifier la PURETE reelle de CHAQUE fonction (grep de l'etat module `COL_*`/`WS_*` dans le corps, pas se fier a un design anterieur) ; (2) faire un controle ANTI-COLLISION global sur TOUT `vba/*.bas` LIVE (hors backups) : tout homonyme deja `Public` ailleurs interdit le passage Public (garder Private dans le module d'origine). VBA n'a pas de namespaces : un Private homonyme (ex. `modCarte.ParseRecords`, signature ≠) est OK (portee module), un Public homonyme ne l'est pas. |
| 2026-07-01 | X44 Phase 2 (decoupe moteur modSyncGS -> modSyncCfg + modSyncEngine) : pour decider si une fonction/const peut passer Public lors d'une extraction, la regle « nom unique » de Phase 1 etait trop grossiere. Un homonyme PRIVATE ailleurs (ex. `SyncSecretQS` dans modSyncParametres) n'empeche PAS de rendre Public (le Private reste local, shadowing) ; seul un homonyme PUBLIC bloque (`SetStatus` Public dans ModuleImportGS, `ToNum` Public dans modSaisie = « Nom ambigu »). | Regle affinee pour passer une fonction/const `Public` a l'extraction VBA : verifier la VISIBILITE des homonymes, pas seulement leur existence. Homonyme Private ailleurs -> Public OK. Homonyme Public ailleurs -> INTERDIT : soit garder Private dans le module d'origine, soit (si le nouveau module en a besoin) lui DUPLIQUER une copie Private locale (ex. `SetStatus` 3 l. recopiee dans modSyncEngine). `grep -E "^Public (Sub|Function|Const) NAME"` sur tout `vba/*.bas` live tranche. Prouver un refactor VBA par Debug->Compile (resout toutes les refs cross-module) + une macro read-only qui exerce le moteur en reel (ici `SyncDiagnose` -> `GS=21|XL=22`), pas seulement l'absence d'erreur d'injection. |
| 2026-07-02 | X44 Phase 3 (decoupe modGraphiques) : mon grep d'etat module `^(Public|Private) (Const|Dim|Type|Enum)` a RATE les variables `Private mPerDeb As Double` / `Private mPerFin As Double` (declarees SANS mot-cle `Dim`, forme `Private <nom> As <type>`). Deplacees dans modGraphData, elles pointaient vers un etat reste dans modGraphiques -> hors de portee -> le module ne compilait plus (compile-on-demand : erreur non trappable a l'appel de BuildAggregates, code -2146827284 « macro indisponible »). | Avant d'extraire des fonctions d'un module VBA, lister TOUTES les declarations niveau module avec `^\s*(Public|Private|Dim|Global)\b` (PAS seulement `... (Const|Dim|Type)`), car une variable module s'ecrit souvent `Private nom As Type` sans `Dim`. Tout etat module mutable partage entre fonctions qui vont dans des modules differents doit devenir `Public` dans le module de config partage (ici `modGraphCfg.mPerDeb/mPerFin`). Une erreur de portee VBA est NON TRAPPABLE (ni On Error Resume Next ni GoTo) et surface en compile-on-demand a l'appel de la 1re fonction du module fautif. |
| 2026-07-02 | X44 Phase 3 (injection COM) : `VBComponents.Remove(c)` est DIFFERE — il ne s'applique qu'au RETOUR du process a Excel, pas immediatement (meme avec PumpWaitingMessages). Faire Remove PUIS Import dans le MEME appel win32com -> l'ancien module existe encore -> Excel cree un module FANTOME `modXxx1` (doublon). Deux modules definissant les memes symboles Public = « Nom ambigu detecte » -> TOUT le projet ne compile plus. ~10 cycles remove/import ont empile Cfg1/Data1/Render1/iques1 et fige le cache de compilation (Debug->Compile finit en E_FAIL). | Pour REMPLACER un module VBA par Import : (1) faire le `Remove` dans une invocation python, SORTIR du process (Excel applique la suppression) ; (2) VERIFIER l'absence dans une 2e invocation ; (3) `Import` dans une 3e. NE JAMAIS Remove+Import dans le meme process. Le plus propre : injecter sur un « slate vide » (aucun module homonyme present) = zero fantome. Si l'etat live est corrompu (fantomes + cache fige), la recuperation fiable est : fermer le classeur SANS enregistrer + rouvrir NORMALEMENT (double-clic, macros actives), puis Import a neuf. Detecter les fantomes : lister les composants dont le nom finit par un chiffre et dont le prefixe existe deja. |
| 2026-07-02 | X44 P3 : apres avoir TUE un script win32com pendant un hang (AddFromString), l'utilisateur a signale « les macros ne se lancent pas ». Cause : le script avait fait `xl.EnableEvents=False` (pour accelerer l'injection) et son bloc `finally`/restauration n'a JAMAIS tourne (process tue) -> Excel est reste `EnableEvents=False`. Les macros appelees directement (Application.Run) marchaient, mais tous les DECLENCHEURS interactifs (boutons OnAction, Worksheet_Change, Workbook_*) etaient geles -> « rien ne se passe » cote utilisateur. | Ne JAMAIS laisser un script COM modifier `Application.EnableEvents/ScreenUpdating/Calculation/Interactive` sans restauration GARANTIE. Si un script COM a ete tue/timeout en cours, TOUJOURS re-verifier et remettre `EnableEvents=True`, `ScreenUpdating=True`, `Calculation=xlCalculationAutomatic(-4105)`, `Interactive=True`, `StatusBar=False` dans un appel dedie avant de rendre la main. Symptome « macros ne se lancent pas » alors que Application.Run marche = EnableEvents=False en priorite a verifier. |
| 2026-07-02 | Reprise « fuel3 » : le zip `session-export-fuel3.zip` portait le nom d'une chaine ancienne, mais son transcript reel etait la session « Fuel » (demande #1 = dates FR + W81+X43c-opt+X44+X45+G1+G3+X53), differente du `mini-export-fuel3.md` deja present dans tasks/ (reprise fuel2, demande W78..S13). Un mini-export homonyme peut appartenir a une session ANTERIEURE. | En reprise, ne JAMAIS se fier au nom du zip ni a un mini-export homonyme : extraire les vrais messages `type=user` du .jsonl (via metadata.json createdAt/lastActivityAt pour dater), et croiser avec `git log` pour etablir le statut par PREUVE. Comparer les dates de fichiers tasks/ vs la date du transcript pour ne pas confondre deux sessions. |
| 2026-07-02 | vba-agent : le script CLI n'est PAS dans `~/.claude/skills/vba-agent/scripts/` mais a la racine `~/.claude/skills/vba-agent/vba_agent.py`. De plus le Python par defaut du PATH (C:\Python312) n'a NI pywin32 NI oletools -> `No module named 'win32com'` / `oletools absent`. | Pour tout appel vba-agent (COM) ou check_vba_drift.py (oletools), utiliser l'interpreteur Python313 complet : `C:/Users/fdaub/AppData/Local/Programs/Python/Python313/python.exe` (le meme que `graphify-out/.graphify_python`). Chemin du CLI : `C:/Users/fdaub/.claude/skills/vba-agent/vba_agent.py`. |
| 2026-07-02 | G1 : le ROADMAP disait « validation de données sur l'onglet de saisie **GS** » mais l'utilisateur voulait en fait l'**Excel** (onglet `Notes`, tables `tbl_carburant`/`tbl_stationEssence`). J'ai d'abord cartographié le Google Sheet (perte de temps) avant que « dans l'onglet existant Notes » ne clarifie la cible. | Pour une feature « saisie/liste/validation », NE PAS présumer Excel vs Google Sheet d'après un sigle du ROADMAP : vérifier factuellement où vit la ressource nommée (ici `tbl_carburant` = ListObject → Excel) via COM/inspection AVANT de concevoir. « tbl_* » = nomenclature ListObject Excel ; un onglet non listé par le code GAS n'existe pas forcément côté Sheet. |
| 2026-07-02 | VBA injection : `set-module` (AddFromString) a HANGÉ 2 min sur un nouveau module → timeout → a laissé Excel instable qui a ouvert un classeur de **récupération AutoRecover** (`…AppData\Roaming\Microsoft\Excel\<nom> (version 1).xlsb`) à côté du vrai .xlsm ; `GetActiveObject` renvoyait alors « Opération non disponible ». | Pour CRÉER/remplacer un module VBA, préférer `import --path vba/Mod.bas` (canonique) dès le départ, PAS `set-module` (AddFromString peut hanger). Si Excel devient instable / un `(version 1).xlsb` apparaît : demander à l'utilisateur de fermer la récupération SANS enregistrer et rouvrir le .xlsm normalement AVANT de reprendre l'automation (ne pas fermer un classeur de récup soi-même = risque de perte). Diag : `vba_agent list` montre les 2 classeurs. |
| 2026-07-02 | VBA `Names.Add RefersTo:="=OFFSET(Notes!$B$3,0,0,MAX(1,COUNTA(...)),1)"` a fait échouer la macro à l'exécution (erreur COM générique) — fragile en locale FR (séparateurs d'arguments virgule/point-virgule). | Pour une plage nommée source de data validation, référencer le CORPS d'une table structurée : `Names.Add nm, "=tbl_xxx"` (le nom de table seul → son DataBodyRange, dynamique, SANS fonction ni séparateur d'argument). Robuste en toute locale. Data validation accepte `Formula1:="=nm"` (nom cross-feuille OK). |
| 2026-07-02 | Vérifier un Sheet via l'API Sheets : le refresh_token OAuth est dans `.claude/gas-config.json` sous **`cfg.oauth.*`** (client_id/client_secret/refresh_token/token_endpoint), PAS à la racine `cfg.*` (mon 1er script → `invalid_client`). Le scope inclut Sheets (lecture spreadsheets.get/values OK). | Pour tout script vérifiant le Google Sheet : lire `cfg.oauth.{client_id,client_secret,refresh_token}`, POST x-www-form-urlencoded sur `oauth2.googleapis.com/token`, puis `sheets.googleapis.com/v4/spreadsheets/{SPREADSHEET_ID}` (id = `1uN170kt…`). Réutiliser la logique de `gas-deploy.mjs` (getAccessToken). |
| 2026-07-02 | Déploiement GAS en prod : malgré l'instruction « réalise G3 », le classifier auto-mode a BLOQUÉ `gas-deploy.mjs` (déploiement production non explicitement autorisé — l'utilisateur avait écarté la question déploiement plus tôt). | Avant tout déploiement GAS en PRODUCTION, obtenir une autorisation EXPLICITE et récente via `AskUserQuestion` (leçon #76) ; une instruction générale « réalise X » ne lève pas la barrière prod. Ne jamais contourner le refus du classifier — redemander à l'utilisateur. |
| 2026-07-02 | C1/G5/G4 : lancer DEUX process `vba_agent` d'ecriture (`set-module`) en concurrence (dont un en background) les a DEADLOCKES sur le VBProject -> les deux hangent >40s -> mal diagnostique en « Excel modal-wedge ». En realite les LECTURES (`inspect`) passaient instantanement = COM sain ; seul `AddFromString` (set-module) bloquait (leçon #98). L'approbation `computer-use request_access` a expire (300s, utilisateur absent) -> impossible de « voir » un pseudo-modal inexistant. | Ne jamais lancer 2 ecritures COM VBProject en parallele (1 seule a la fois, jamais en background concurrent). Un `set-module`/`AddFromString` qui hangue alors que `inspect` (lecture) repond = PAS un modal Excel : c'est le hang connu d'AddFromString (leçon #98/#107) -> basculer sur `remove` (proc 1, sortir) puis `import` (proc 2), en process SEPARES (leçon #102). Tuer les python bloques AVANT de reessayer (ils tiennent le lock). Ne pas invoquer computer-use pour diagnostiquer un « modal » sans preuve (les lectures COM saines l'infirment). |
| 2026-07-02 | C1/G5 : `import` (VBComponents.Import) nomme le composant d'apres la ligne `Attribute VB_Name` du .bas -> or `vba/ModuleImportGS.bas` est committe SANS cette ligne (incoherent avec les autres .bas qui l'ont). Import sans Attribute aurait mal nomme le module. | Avant un `import`, VERIFIER que le .bas porte `Attribute VB_Name = "<nom>"` en L1 ; s'il manque (cas ModuleImportGS), creer une copie temp (chemin COURT, leçon #78) avec la ligne ajoutee. NB : `import` CONSOMME cette ligne (module propre, pas d'`Attribute` parasite dans le corps) — inverse de `set-module`/`AddFromString` qui exige de la STRIPPER (leçon #91). Recette fiable de remplacement verifiee ce jour : remove x4 (1 proc) -> verifier absence (1 proc) -> import x4 depuis temp court (1 proc) -> inspect (fantomes/L1/edits) -> run public read-only (FuelKeyP, prouve compile + C1) -> save. |
| 2026-07-03 | Reprise fuel6 : `graphify --update` (detect_incremental) N'A PAS listé les fichiers `vba/*.bas` modifiés (modSyncGS trimmé, modSyncDiag créé) — les `.bas` ne sont pas une extension scannée par graphify. `grep modSyncDiag graph.json`=0 avant update alors que le module était sur disque. Les concepts VBA n'entrent dans le graphe QUE via l'extraction sémantique des docs (CHANGELOG/ROADMAP qui les mentionnent). | Pour un commit VBA-only, ne pas s'étonner que `graphify --update` ne détecte « rien à extraire » côté `.bas` : documenter le module dans CHANGELOG/ROADMAP suffit à le faire entrer dans le graphe via l'extraction doc. Le gate pré-commit reste satisfait (graphe régénéré, anti-amputation OK) même si le `.bas` lui-même n'est pas un nœud AST. |
| 2026-07-03 | Reprise fuel6 : session précédente coupée pile pendant le gate graphify final du module 4 → travail 100% sur disque (modSyncDiag.bas, modSyncGS trimmé, APP_VERSION/CHANGELOG/ROADMAP à 5.31.4.0) mais NON commité (HEAD=origin à module 3). Scénario « mi-graphify » de la mémoire, confirmé. | Recette de reprise validée : (1) `git status` → repérer working-tree bumpé non commité ; (2) backup graph.json + relevé nœuds ; (3) `graphify --update` (build_merge fusionne, ne peut qu'ajouter) ; (4) vérifier nœuds ≥ avant (2491→2495) ; (5) `commit.sh`. Ne PAS relancer un build complet : `build_merge` préserve l'existant. |
| 2026-07-26 | Reprise « rentabilité » (X66) : `vba_agent set-module` échouait en `[WinError 3] …\<code-file>\gen_py` (chemin pourtant COURT `C:\…\Temp\mg_body.bas`), purge de `%TEMP%\gen_py` sans effet. Cause : `CodeModule.AddFromString` déclenche une génération early-binding makepy (gen_py) qui déraille sur cet environnement (séquelle des kills COM de la session morte) ; les lectures (`list`/`inspect`) en late-binding passaient pourtant sans souci. Diag confirmé par lecture de `vba_agent.py` (aucun code gen_py custom) + `win32com.__gen_path__` correct (`Temp\gen_py\3.13`). | Quand `vba_agent set-module`/`AddFromString` échoue en `[WinError 3] …gen_py` alors que `list`/`inspect` marchent, NE PAS s'acharner sur le cache gen_py : écrire un **script win32com maison en LATE-BINDING** (`win32com.client.GetActiveObject('Excel.Application')` → `wb.VBProject.VBComponents` → `CodeModule.DeleteLines`+`AddFromString`) — le late-binding ne déclenche jamais makepy/gen_py. Sauvegarder le module (`cm.Lines`) AVANT DeleteLines, vérifier `CountOfLines`+absence de doublons APRÈS, prouver la compilation par un `Application.Run` réel (ici `ExportChartPNG("gKitProj")` → 9890 o ≠ 0). |
| 2026-07-26 | Déploiement VBA via script maison : le code-file écrit en Python text-mode APRÈS avoir joint les lignes avec `\r\n` → le mode texte Windows retraduit `\n`→`\r\n` → `\r\r\n` sur disque ; relu en universal-newlines puis `AddFromString` → **doublement des lignes** (902 au lieu de 450, ~450 lignes vides fantômes). Compile quand même mais module pollué. | Pour un code-file VBA : l'écrire en **binaire** (`open(...,'wb')`) avec des `\r\n` EXACTS (jamais de double), et dans le script de déploiement **re-normaliser** avant `AddFromString` : `code = raw.replace('\r\n','\n').replace('\r','\n').replace('\n','\r\n')`. Vérifier après injection que `CountOfLines` ≈ nb de lignes source (un écart ~×2 = doublement CRLF). Contrôle CR count == LF count sur le fichier avant déploiement. |
| 2026-08-14 | Réécriture de `js/rentabilite.js` (messages bannière + imports) sans avoir d'abord repéré le `tests/rentabilite.test.js` EXISTANT ; risque de casser un test en place. Détecté tard (via graphify), pas via une vérif amont. | Avant de réécrire/modifier un module JS, GREPPER `tests/` pour un fichier de test le ciblant (`grep -rl "from '../js/<module>.js'" tests/`) et le relancer après. Un module « anodin » de 55 l. peut avoir son test. |
| 2026-08-14 | Données de test avec un plein baseline à `Km compteur = 0` : `computeConsoMoy`/`resolveConsoDiesel` filtrent `Km compteur > 0` → le 1er plein est écarté, la conso mesurée retombe sur le défaut (5,5) au lieu de la valeur attendue → 2 tests faux au 1er run. | Pour tester un calcul de conso basé sur des deltas km, utiliser des km de baseline NON NULS (ex. 1000→1500), jamais 0→N : le 0 est filtré comme « pas de compteur ». Le comportement du code (ignorer km=0) est correct ; c'est la donnée de test qui doit être réaliste. |
| 2026-08-14 | `vba-agent set-module` sur un module ~490 l. **hang** (>120s, 2 fois) sur machine chargée (mêmes conditions que les timeouts vitest forks) ; python tué → module resté intact (le hang précède l'écriture, pas de corruption cette fois). J'ai bouclé une 2e fois avant d'abandonner. | Écriture COM VBE qui hang = **environnemental** (charge machine), pas un bug de code (cf. #46/#54). NE PAS boucler : après 1 hang borné (~120s), tuer python (jamais Excel, #34), VÉRIFIER l'intégrité du module (`inspect --names-only` : lignes>0 = intact), puis soit fermer le volet VBE ouvert et réessayer une fois, soit DIFFÉRER l'écriture VBA à une session peu chargée. Reverter toute édition disque `.bas` non déployée pour garder disque==live (#51). L'inspect COMPLET (dump source) échoue en cp1252 (`charmap can't encode`) → préfixer `PYTHONIOENCODING=utf-8`. |
| 2026-08-30 | La version affichée dans l'app est restée figée à `v5.32.4.0` sur ~7 versions successives : `APP_VERSION` (js/config.js) n'était jamais remontée. `commit.sh` traite `APP_VERSION` comme source de vérité éditée à la main et se contente d'un WARN en cas de divergence (il n'aligne QUE package.json). Pire : `vite.config.js getVersion()` lit `APP_VERSION` pour nommer le cache SW (`shell-v<APP_VERSION>`) → cache jamais renommé → clients bloqués sur l'ancien shell entier, pas juste le libellé. | À CHAQUE bump `[vX.Y.Z.W]`, éditer À LA MAIN `APP_VERSION` dans `js/config.js` AVANT `./commit.sh` (le script ne le fait pas, il avertit seulement). Ne pas confondre avec `package.json` (aligné auto). `APP_VERSION` pilote 2 choses : l'affichage (`main.js:48`) ET le cache-busting du service worker (`vite.config.js:9`). Une divergence = déploiements invisibles côté client. Toujours lire les WARN de l'étape 3 de commit.sh (ne pas `tail` en coupant la synchro version). |
